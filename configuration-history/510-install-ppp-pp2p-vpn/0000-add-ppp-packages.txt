# install base packages (all but pppconfig should be already there)
apt install ppp pppoeconf pppconfig 

# install pptp protocol for ms-chap VPN
apt install pptp-linux

# install kde UI 
# apt-get install kppp 
